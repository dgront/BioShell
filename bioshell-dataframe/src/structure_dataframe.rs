use std::collections::hash_map::DefaultHasher;
use std::hash::{Hash, Hasher};
use std::convert::TryFrom;

#[derive(Debug, PartialEq, Eq, Hash)]
pub struct StructureDataFrame {

}

impl StructureDataFrame {
}